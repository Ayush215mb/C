#include<stdio.h>
int main()
{
    //we can declare single integer and array together in a single line
    int x=2,y=5,m[4],u=5;

    //we can also declare float array
    float z[8],w=9.4, i[2],p=8893.334;

    int size=10, m[size];

    printf("%f",p);
}